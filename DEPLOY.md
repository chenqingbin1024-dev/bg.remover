# 部署指南

本项目可以部署到多个平台，以下是推荐的部署方案。

## 🚀 方案一：Render（推荐，最简单）

### 步骤：

1. **注册 Render 账号**
   - 访问 https://render.com
   - 使用 GitHub 账号登录（推荐）

2. **准备代码仓库**
   - 将代码推送到 GitHub（如果还没有）
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin <你的GitHub仓库地址>
   git push -u origin main
   ```

3. **在 Render 创建 Web Service**
   - 登录 Render Dashboard
   - 点击 "New +" → "Web Service"
   - 连接你的 GitHub 仓库
   - 配置如下：
     - **Name**: `bg-remover`（或你喜欢的名字）
     - **Environment**: `Node`
     - **Build Command**: `npm install`
     - **Start Command**: `npm start`
     - **Plan**: 选择 Free（免费）

4. **配置环境变量**
   - 在 Render Dashboard 的 Environment 标签页
   - 添加环境变量：
     - **Key**: `REMOVE_BG_API_KEY`
     - **Value**: 你的 remove.bg API Key

5. **部署**
   - 点击 "Create Web Service"
   - Render 会自动构建和部署
   - 等待几分钟，你会得到一个类似 `https://bg-remover.onrender.com` 的 URL

---

## 🚂 方案二：Railway

### 步骤：

1. **注册 Railway 账号**
   - 访问 https://railway.app
   - 使用 GitHub 账号登录

2. **创建新项目**
   - 点击 "New Project"
   - 选择 "Deploy from GitHub repo"
   - 选择你的仓库

3. **配置环境变量**
   - 在项目设置中找到 "Variables"
   - 添加：`REMOVE_BG_API_KEY` = 你的 API Key

4. **Railway 会自动检测并部署**
   - 它会自动识别 `package.json` 和 `start` 脚本
   - 部署完成后会给你一个 URL

---

## ⚡ 方案三：Vercel（需要适配）

Vercel 主要面向 serverless，需要稍微调整代码结构。如果你选择这个方案，我可以帮你创建适配文件。

---

## 📝 部署前检查清单

- [ ] 确保 `.env` 文件已添加到 `.gitignore`（已包含）
- [ ] 确保 `node_modules` 已添加到 `.gitignore`（已包含）
- [ ] 确保 `pic_origin.jpg` 和 `pic_process.jpg` 已上传到 `public` 目录
- [ ] 准备好你的 `REMOVE_BG_API_KEY`

---

## 🔧 常见问题

### 1. 部署后图片无法加载？
- 确保 `pic_origin.jpg` 和 `pic_process.jpg` 已提交到 Git 仓库
- 检查文件路径是否正确（应该是 `/pic_origin.jpg`）

### 2. API 调用失败？
- 检查环境变量 `REMOVE_BG_API_KEY` 是否正确配置
- 检查 remove.bg API 的调用次数限制

### 3. 端口问题？
- Render/Railway 会自动设置 `PORT` 环境变量，代码已支持

---

## 💡 推荐

**对于这个项目，我推荐使用 Render**，因为：
- ✅ 免费层可用
- ✅ 配置简单
- ✅ 自动 HTTPS
- ✅ 支持自定义域名
- ✅ 自动部署（Git push 后自动更新）

